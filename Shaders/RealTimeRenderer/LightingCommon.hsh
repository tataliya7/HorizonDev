struct Lighting
{
    float3 total;
    float3 diffuse;
    float3 specular;
};

void AccumulateLighting(inout Lighting lighting, float3 diffuse, float3 specular)
{
    lighting.diffuse += diffuse;
    lighting.specular += specular;
    lighting.total += (diffuse + specular);
}